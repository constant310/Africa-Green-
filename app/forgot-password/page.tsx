'use client';
import Link from 'next/link';import {FormEvent,useState} from 'react';import {supabase} from '@/lib/supabase';
export default function Forgot(){const[email,setEmail]=useState('');const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMsg('');const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${location.origin}/update-password`});setBusy(false);setMsg(error?error.message:'Password reset instructions have been sent if that email is registered.')}return <main id="main" className="authSolo"><form className="authCard" onSubmit={submit}><Link className="brandLink" href="/"><span className="publicLogo">AD</span><span><b>Acres of Diamond</b><small>Multipurpose Cooperative Society</small></span></Link><p className="eyebrow authTop">ACCOUNT RECOVERY</p><h1>Reset password</h1><p className="muted">Enter the email address attached to your account.</p><label>Email address<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label>{msg&&<div className="alert">{msg}</div>}<button className="btn primary wide" disabled={busy}>{busy?'Sending…':'Send reset link'}</button><p className="center small"><Link href="/login">Back to login</Link></p></form></main>}
