'use client';
import { FormEvent,useState } from 'react';
import { supabase } from '../../lib/supabase';
export default function Register(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMsg('');const{error}=await supabase.auth.signUp({email,password,options:{emailRedirectTo:'https://cooperative-production-v2.vercel.app/portal'}});setBusy(false);setMsg(error?error.message:'Account created. Check your email to verify your account.');}return <main className="authPage"><section className="authVisual"><div className="logoBox">AD</div><span className="eyebrow">JOIN THE COOPERATIVE</span><h1>Turn regular contributions into real progress.</h1><p>Create your account, complete onboarding and manage your cooperative relationship from anywhere.</p></section><section className="authPanel"><form className="authCard" onSubmit={submit}><h2>Create account</h2><p className="muted">Use an email address you can access.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required/></label>{msg&&<div className="alert">{msg}</div>}<button className="btn primary wide" disabled={busy}>{busy?'Creating…':'Create account'}</button><p className="small muted center">Already registered? <a href="/login">Sign in</a></p></form></section></main>}
