'use client';
import {FormEvent,useState} from 'react';import Link from 'next/link';import {supabase} from '@/lib/supabase';
export default function UpdatePassword(){const[password,setPassword]=useState('');const[confirm,setConfirm]=useState('');const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');async function submit(e:FormEvent){e.preventDefault();if(password!==confirm)return setMsg('Passwords do not match.');setBusy(true);const{error}=await supabase.auth.updateUser({password});setBusy(false);setMsg(error?error.message:'Password updated. You can return to your portal.')}return <main id="main" className="authSolo"><form className="authCard" onSubmit={submit}><p className="eyebrow">SECURITY</p><h1>Choose a new password</h1><label>New password<input type="password" autoComplete="new-password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Confirm password<input type="password" autoComplete="new-password" minLength={8} required value={confirm} onChange={e=>setConfirm(e.target.value)}/></label>{msg&&<div className="alert">{msg}</div>}<button className="btn primary wide" disabled={busy}>Update password</button><p className="center small"><Link href="/portal">Return to portal</Link></p></form></main>}
